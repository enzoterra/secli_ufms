import 'package:flutter/material.dart';

class CustomIcons {
  Icon arrowback = const Icon(Icons.arrow_back_rounded);
  Icon settings = const Icon(Icons.settings);
  IconData botnav1 = Icons.devices;
  IconData botnav2 = Icons.support_agent;
  IconData botnav3 = Icons.manage_accounts;
}
